name: Build NetDoctor DLL

on:
  workflow_dispatch:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        platform: [x64, x86]
        configuration: [Release]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build
        shell: pwsh
        run: |
          msbuild NetDoctor.sln `
            /m `
            /p:Configuration=${{ matrix.configuration }} `
            /p:Platform=${{ matrix.platform }} `
            /p:RestorePackages=false `
            /p:PreferredToolArchitecture=x64 `
            /verbosity:minimal

      - name: List build outputs
        if: always()
        shell: pwsh
        run: |
          Get-ChildItem -Recurse -Include *.dll,*.lib,*.obj,*.log,*.tlog -ErrorAction SilentlyContinue | Select-Object FullName,Length | Format-Table -AutoSize

      - name: Collect artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifact | Out-Null
          Get-ChildItem -Recurse -Filter NetDoctor.dll | ForEach-Object {
            $target = "artifact/NetDoctor-${{ matrix.platform }}.dll"
            Copy-Item $_.FullName $target -Force
            Write-Host "Copied $($_.FullName) -> $target"
          }
          if (-not (Test-Path artifact/*.dll)) { throw "NetDoctor.dll was not found after build" }
          Get-ChildItem artifact

      - name: Upload DLL
        uses: actions/upload-artifact@v4
        with:
          name: NetDoctor-${{ matrix.platform }}-${{ matrix.configuration }}
          path: artifact/*.dll
